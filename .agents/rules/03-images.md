---
trigger: always_on
---

Local Assets: We will use Astro's native <Image /> component or getImage() function for all local images to automatically handle optimization, resizing, and modern formats (WebP/AVIF), guaranteeing the fast load times needed for those local SEO analytics.
